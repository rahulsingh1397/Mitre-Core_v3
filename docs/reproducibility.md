# Reproducibility

MITRE-CORE V3 reproducibility requirements:

- pinned dependencies with upper bounds
- deterministic seed control
- benchmark outputs committed with code
- CI smoke benchmark
- model card and checkpoint metadata tracked in-repo
