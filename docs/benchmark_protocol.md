# MITRE-CORE V3 Benchmark Protocol

This document mirrors `BENCHMARK.md` and serves as the longer-form benchmark narrative for the repository.

## Fair Evaluation Rules

- shared frozen splits
- shared seed set
- shared metric implementation
- shared hyperparameter budget
- negative results retained

## Benchmark Intent

The benchmark is designed to test where heterogeneous graph modeling helps and where simpler methods remain competitive.
