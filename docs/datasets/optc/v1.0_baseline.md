# DARPA OpTC — Frozen Baseline v1.0

> Frozen: 2026-05-21  
> Tag: `darpa-optc-v1.0`  
> Path: `benchmark/results/frozen/optc/v1.0/`

> ## ⚠️ 2026-05-24 LABEL-LEAKAGE AUDIT — Validity classification
>
> **Status under v1.0 Integrity Audit (Stage B materiality test):** **CATASTROPHIC**
>
> | Reported ARI | Clean ARI | Δ |
> |---|---|---|
> | 0.999 binary_ari | −0.020 | −1.019 |
>
> **Without the label leakage, the model cannot separate Benign from RedTeam at all.** Clean binary_ari = −0.020 is below random (a coin flip would score 0.0). The 0.999 reported "near-perfect" result was 100% leakage. OpTC v1.0 cannot be reported as a real V3 result. The OpTC retrain candidates listed elsewhere in this program (`multidomain_v2_optc_finetuned`) are suspected to inherit the same input pipeline and likely show similar collapse under clean measurement — flagged for v2.0 priority testing.
>
> The reported numbers throughout this document were measured with the leaked
> baseline (`tactics` and `alert_types` fed as inputs). The clean ARI is the
> result of shuffling those two input columns at inference, leaving everything
> else unchanged. See `experiments/results/leakage_materiality.csv` for raw evidence,
> `docs/audits/v1.0_input_feature_audit.md` for the full report, and
> `docs/plans/MASTER_PLAN_v1.2.md` for the validity ledger across all freezes.
>
> Frozen artifacts in `benchmark/results/frozen/optc/v1.0/` are immutable
> reproducibility records — they document what the pipeline produced, not validated
> performance. v2.0 clean-baseline retrain is queued; until then, when comparing
> V3 to honest baselines, use the clean ARI.

---

## Headline Metric

**binary_ARI** (not standard ARI) — standard ARI is structurally low on 2-class datasets because fine-grained sub-clustering within the two true campaigns produces chance-level ARI by design.

## Results — Campaign ID Track (binary)

| Method | binary_ARI | AMI | ARI | Purity | attack_f1 |
|--------|-----------|-----|-----|--------|-----------|
| **MITRE-CORE V3** | **0.9993** | 0.398 | 0.255 | 0.9999 | 0.999 |
| HDBSCAN (emb) | **0.9993** | 0.305 | 0.213 | 0.9999 | 0.999 |
| HDBSCAN (raw) | 0.9973 | 0.411 | 0.270 | 0.9998 | 0.998 |
| PCA + HDBSCAN | 0.9750 | 0.259 | 0.390 | 0.9979 | 0.980 |
| PCA + K-Means | 0.5000 | 0.555 | 0.538 | 0.9790 | 0.500 |
| Spectral (raw) | 0.0367 | 0.026 | 0.036 | 0.8052 | 0.538 |
| DBSCAN (raw) | 0.0151 | 0.013 | 0.010 | 0.8060 | 0.512 |
| Spectral (emb) | 0.0266 | 0.133 | 0.017 | 0.8045 | 0.500 |
| K-Means (emb) | 0.0000 | 0.025 | 0.022 | 0.8032 | 0.500 |
| K-Means (raw) | 0.0004 | 0.001 | 0.002 | 0.8035 | 0.500 |

Table ordered by **binary_ARI** (headline metric).

Notes:
- V3 and HDBSCAN(emb) tie at binary_ARI = 0.9993 — both use V3 embeddings.
- HDBSCAN(raw) achieves 0.9973 without embeddings, showing the raw features already have strong separation.
- PCA + K-Means gets 0.5000 binary_ARI because `n_clusters=2` gives it privileged information, yet it still performs no better than random on the binary task.
- Standard ARI is ~0.05 for most methods by design on a 2-class dataset.

## Results — Tactic Track

| Method | binary_ARI | AMI | ARI |
|--------|-----------|-----|-----|
| MITRE-CORE V3 | 1.0000 | 0.593 | 0.449 |
| HDBSCAN (emb) | 1.0000 | 0.520 | 0.388 |
| PCA + HDBSCAN | 1.0000 | 0.563 | 0.422 |
| HDBSCAN (raw) | 1.0000 | 0.638 | 0.497 |
| Spectral (emb) | 1.0000 | 0.523 | 0.423 |
| Spectral (raw) | 1.0000 | 0.519 | 0.418 |
| K-Means (emb) | 1.0000 | 0.477 | 0.381 |
| K-Means (raw) | 1.0000 | 0.534 | 0.433 |
| PCA + K-Means | 1.0000 | 0.534 | 0.433 |
| DBSCAN (raw) | 1.0000 | 0.441 | 0.347 |

Note: The tactic column is degenerate — only 1 unique non-null value (Execution), with NaN filled as "UNKNOWN". This creates a 2-class track where all methods trivially achieve binary_ARI = 1.0. The tactic track is documented for completeness but is **not a meaningful benchmark track**.

## Supervised Reference

| Checkpoint | val_ari (supervised) |
|------------|---------------------|
| multidomain_v2_optc_finetuned | 0.818 |

This is the supervised training reference only — **not part of the unsupervised benchmark**.

## Protocol Parameters

| Parameter | Value |
|-----------|-------|
| Checkpoint | `hgnn_checkpoints/multidomain_v2_optc_finetuned/best_supervised.pt` |
| Dataset | `datasets/DARPA_OpTC/processed_optc_full.csv` |
| sample_size | 10000 |
| sample_seed (eval) | 142 |
| sample_seed (dev) | 42 |
| stratified_sample | true |
| n_clusters | 2 |
| clustering_method | hdbscan |
| use_geometric_confidence | true |

## Decision

Path A — V3 binary_ARI = 0.9993 (tied with HDBSCAN(emb)), >0.1 margin over all baselines except HDBSCAN(emb) which also uses V3 embeddings. No sweep required.

## Metric Notes

- **dominant_confusion_accuracy**: pre-demoted (degenerate on all 6 datasets; returns 1.0 for all methods)
- **Standard ARI**: structurally low on 2-class datasets; not the primary metric
- **binary_ARI on tactic**: 1.0 for all methods (degenerate track due to single tactic value)
- All 12 metrics present in `results.csv`
