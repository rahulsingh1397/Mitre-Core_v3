# NSL-KDD — v1.0 Frozen Baseline

> **Status:** FROZEN — 2026-05-19
> **Master plan section:** Part I (NSL-KDD v1.0 Freeze) + Part IV.1
> **Git tag:** `nsl-kdd-v1.0`
> **Immutability:** This document and `benchmark/results/frozen/nsl_kdd/v1.0/` are read-only.
> Any re-evaluation (new engine config, new checkpoint, new label tracks) produces v1.1 in a new folder.

---

## Reproduction Command

```bash
python -m benchmark.run_benchmark \
  --methods benchmark/methods.yaml \
  --datasets benchmark/datasets_real.yaml \
  --output benchmark/results/latest/nsl_kdd/results.csv
```

Expected output matches `benchmark/results/frozen/nsl_kdd/v1.0/summary.csv` within tolerance 1e-3 for all deterministic methods.

---

## Dataset

| Field | Value |
|---|---|
| Source | `datasets/nsl_kdd/mitre_format.csv` |
| Source rows | 125,973 |
| SHA256 | `29d0919999e2a1cc952a5cc6826b0906d3b5862108f68dc151e739415e4bb706` |
| Eval split | `benchmark/splits/nsl_kdd_10000_seed142.npy` (10,000 rows, seed 142) |
| Dev split | `benchmark/splits/nsl_kdd_10000_seed42.npy` (10,000 rows, seed 42) — tuning only |
| Split disjoint | Yes — dev and eval indices do not overlap |
| Benchmark seeds | 42, 43, 44 |
| Primary label track | `tactic` (8 classes) |

---

## Checkpoint

| Field | Value |
|---|---|
| Path | `hgnn_checkpoints/network_v9_v3/network_it_best.pt` |
| SHA256 | `c74da9b1ca6f1b7439d7a71480d0cd5da3a900df7178eb4587694734dfc48b4e` |
| Training mode | SSL (topological NT-Xent + SimCLR hybrid), zero-shot at inference |

---

## Engine Configuration

```yaml
num_layers: 1
hdbscan_min_cluster_size: 5
hdbscan_pca_components: 16
hdbscan_cluster_selection_epsilon: 0.1
use_geometric_confidence: true
device: cpu
```

---

## Results — Tactic Track (eval split, 3 seeds: 42 / 43 / 44)

Source: `benchmark/results/frozen/nsl_kdd/v1.0/results.csv`, filtered to `label_track == label_col`
(= tactic, the primary track). Mean and std computed over seeds 42/43/44.

> **Note on summary.csv format:** The NSL-KDD v1.0 frozen `summary.csv` was generated by a
> pre-UNSW version of the benchmark runner that averaged across all three label tracks
> (tactic + alert_type + campaign_id) into a single row per method. It has no `label_track`
> column. The canonical per-track numbers are in `results.csv` (used below).
> Later datasets (UNSW-NB15, TON_IoT) produce per-track summary rows.

| Method | AMI mean ± std | ARI mean ± std | Purity mean ± std | attack_f1_demoted |
|---|---|---|---|---|
| **MITRE-CORE V3** | **0.714 ± 0.000** | **0.632 ± 0.000** | **0.991 ± 0.000** | 1.0 |
| Spectral (raw) | 0.514 ± 0.005 | 0.420 ± 0.025 | 0.907 ± 0.000 | 1.0 |
| PCA + K-Means | 0.433 ± 0.010 | 0.302 ± 0.018 | 0.875 ± 0.000 | 1.0 |
| Spectral (emb) | 0.496 ± 0.001 | 0.228 ± 0.001 | 0.944 ± 0.000 | 1.0 |
| PCA + HDBSCAN | 0.344 ± 0.000 | 0.191 ± 0.000 | 0.930 ± 0.000 | 1.0 |
| K-Means (emb) | 0.399 ± 0.001 | 0.189 ± 0.000 | 0.888 ± 0.000 | 1.0 |
| HDBSCAN (emb) | 0.271 ± 0.000 | 0.046 ± 0.000 | 0.940 ± 0.000 | 1.0 |
| HDBSCAN (raw) | 0.000 ± 0.000 | 0.005 ± 0.000 | 0.571 ± 0.000 | 1.0 |
| K-Means (raw) | ~0 | ~0 | 0.535 ± 0.000 | 1.0 |
| DBSCAN (raw) | 0.000 ± 0.000 | 0.000 ± 0.000 | 0.535 ± 0.000 | 0.0 ← demoted (1 cluster) |

**V3 margin over best baseline (Spectral raw):** +0.211 ARI, +0.200 AMI.

---

## Results — Alternate Label Tracks (dev split seed 42, single seed — indicative only)

These numbers are from the Phase 2 dev run (seed 42), not the frozen eval set.
They are recorded here for research context; the authoritative headline is the tactic track above.

| Track | MITRE-CORE V3 AMI | MITRE-CORE V3 ARI |
|---|---|---|
| `tactic` (8 classes) | 0.714 | 0.632 |
| `alert_type` (binary) | 0.557 | 0.501 |
| `campaign_id` (15 classes) | 0.784 | 0.675 |

`campaign_id` appears to be the cleanest label track (highest ARI), suggesting the `tactic` MITRE mapping merges some structurally distinct campaigns.

---

## Metric Notes

| Metric | Status | Note |
|---|---|---|
| AMI | Primary | Main ranking metric |
| ARI | Secondary | Co-primary for comparison |
| Purity | Secondary | High across all methods; less discriminative |
| Silhouette (cosine) | Diagnostic | Negative for MITRE-CORE V3 (−0.54); does not reflect cluster quality accurately on NSL-KDD graph embeddings |
| attack_f1 | Non-discriminative | ~1.0 for all methods; ignored in ranking |
| attack_f1_demoted | Diagnostic | Correctly identifies trivial clusterings (DBSCAN raw = 0.0) |

---

## Known Limitations

1. **Single checkpoint** — results are tied to `network_v9_v3/network_it_best.pt`. New checkpoints require a new version.
2. **Single label track as headline** — `tactic` collapses dissimilar attacks; `campaign_id` may be a more valid track. Re-evaluation with a primary on `campaign_id` would be v1.1.
3. **Phase 3 sweep methodology debt** — a standalone HDBSCAN sweep was run during development and showed catastrophic ARI degradation when ported to the full engine. The current config was hand-selected and validated, not grid-searched through the full engine. `benchmark/clustering_sweep_full_engine.py` exists as the correct future sweep path.
4. **No heavy GNN baselines yet** — HGT, HAN, R-GCN, MAGIC, FLASH not included. When added, this becomes v1.1 with a retro comparison.
5. **CPU-only latency** — 7.9 s/10k measured on CPU; GPU path was not exercised (peak_gpu_gb = 0.0).

---

## Frozen Artifacts

All files in `benchmark/results/frozen/nsl_kdd/v1.0/` are read-only references:

```
v1.0/
├── results.csv              # Per-seed raw results (10 methods × 3 seeds)
├── summary.csv              # Mean ± std aggregated results
├── manifest.json            # Full provenance (hashes, env, command, hardware)
├── engine_config.yaml       # Engine kwargs locked at freeze
├── checkpoint_sha256.txt    # Checkpoint integrity check
├── dataset_sha256.txt       # Dataset integrity check
├── environment.txt          # Python package versions
├── nsl_kdd_10000_seed42.npy   # Dev split indices
├── nsl_kdd_10000_seed42.json  # Dev split metadata
├── nsl_kdd_10000_seed142.npy  # Eval split indices (used for these results)
└── nsl_kdd_10000_seed142.json # Eval split metadata
```

---

## Execution History

Full chronological record: [IMPROVEMENT_LOG.md](../../../IMPROVEMENT_LOG.md) lines 43–261.
Per-dataset decisions: [decision_log.md](decision_log.md).
Lessons: [learnings.md](learnings.md).
